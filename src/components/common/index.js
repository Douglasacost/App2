import Button from './Button';
import Box from './Box';
import Content from './Content';
import Logo from './Logo';
const Components = {
    Button,
    Box,
    Content,
    Logo,
};

export default Components;